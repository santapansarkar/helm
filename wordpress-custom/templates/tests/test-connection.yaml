apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordpress-custom.fullname" . }}-test-connection"
  labels:
    {{- include "wordpress-custom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordpress-custom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
